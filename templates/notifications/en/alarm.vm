#set($subject = "$esc.html($device.name): alarm!")
#set($alarmKey = $event.getString('alarm'))
#set($alarmName = $translations.get("alarm${alarmKey.substring(0, 1).toUpperCase()}${alarmKey.substring(1)}"))
#set($digest = "$device.name alarm: #{if}($alarmKey == 'temperature')Temperature ($position.attributes.temperature&deg;C)#{else}$alarmName#{end} at $dateTool.format('yyyy-MM-dd HH:mm:ss', $event.eventTime, $locale, $timezone)")
<!DOCTYPE html>
<html>
<body>
Device: $esc.html($device.name)<br>
Alarm: #{if}($alarmKey == "temperature")Temperature<br>Current Temperature: $position.attributes.temperature&deg;C#{else}$esc.html($alarmName)#{end}<br>
Time: $dateTool.format('yyyy-MM-dd HH:mm:ss', $event.eventTime, $locale, $timezone)<br>
Point: <a href="https://maps.google.com/?q=$position.latitude,$position.longitude&ll=$position.latitude,$position.longitude&z=18">#{if}($position.address)$position.address#{else}$position.latitude&deg;, $position.longitude&deg;#{end}</a><br>
<br>
<a href="$webUrl/settings/notifications?token=$token">Unsubscribe</a>
</body>
</html>
